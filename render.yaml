services:
  - type: web
    name: structured-qa-tool
    runtime: python
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"
    plan: free
    rootDir: backend